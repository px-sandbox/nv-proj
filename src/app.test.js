describe('ClientController (e2e)', () => {


	it('/client (POST) create client', async () => {
		expect(2).toBe(2);
    });

    it('/client (POST) create client', async () => {
		expect(2).toBe(2);
    });

    it('/client (POST) create client', async () => {
		expect(2).toBe(2);
    });

    it('/client (POST) create client', async () => {
		expect(2).toBe(2);
    });

    it('/client (POST) create client', async () => {
		expect(2).toBe(2);
    });

    it('/client (POST) create client', async () => {
		expect(2).toBe(2);
    });

    it('/client (POST) create client', async () => {
		expect(2).toBe(2);
    });


});